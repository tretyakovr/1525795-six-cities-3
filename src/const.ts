export const Settings = {
  OffersCount: 100,
};

export enum AuthStatus {
  Auth,
  NoAuth,
  Unknown,
}

export enum AppRoutes {
  FavoritesEmpty,
  Favorites,
  Login,
  Main,
  MainEmpty,
  Offer,
  OfferNotLogged,
  Page404,
}
